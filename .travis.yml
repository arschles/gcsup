language: generic
cache:
  directories:
    - vendor
sudo: required
services:
  - docker
script:
  - make test
  - make build
deploy:
  provider: script
  script: ./docker-deploy.sh
  on:
    all_branches: true
